# Stub functions for APScheduler job restoration
# These functions are not actually executed during migration,
# they are only needed so APScheduler can restore jobs from the database


def report_schedule(*args, **kwargs):
    """
    Stub function for report_schedule.
    This function is never actually called during migration.
    """
    pass
